{
    "requires": ["bare-metal"],
    "target_overrides": {
        "*": {
            "target.c_lib": "std",
            "platform.minimal-printf-enable-floating-point": true,
            "target.printf_lib": "minimal-printf"
        }
    }
}
